[package]
name = "miningsim"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.15"
bevy_replicon = "0.32"
bevy_replicon_renet = "0.9"
rand = "0.8"
ron = "0.8"
serde = { version = "1", features = ["derive"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
